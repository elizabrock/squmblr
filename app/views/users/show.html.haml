%h1 Your Profile
= image_tag @user.gravatar_url
%h3= @user.username
%h4= @user.email

%h1 Your squmbls:
%ul.post
  - @posts.each do |post|
    .post
      You squmbd:
      = post.content
      = post.created_at
