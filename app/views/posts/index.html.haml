%h1 These are your squmbls:
%ul.post
  - @posts.each do |post|
    .post
      = post.user.username
      squmbd:
      = post.content
      = post.created_at
    .rating
      = render :partial => 'posts/ratings', locals: {:post => post}
- if user_signed_in?
  = link_to 'new squmbl', new_post_path
