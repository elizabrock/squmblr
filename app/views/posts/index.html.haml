%h1 These are your squmbls:
%ul.post
  - @posts.each do |post|
    .post
      = post.user.username
      squmbd:
      = post.content
      = post.created_at
      = render partial: "social_buttons/index_buttons", :locals => { :post_id => post.id }
- if user_signed_in?
  = link_to 'new squmbl', new_post_path
